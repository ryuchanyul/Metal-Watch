# 로컬에서 scripts/collect.js 테스트 시 .env에 복사 후 채워 사용한다.
# 실제 .env 파일은 .gitignore로 차단되어 커밋되지 않음.

# Supabase 프로젝트 설정 → API 페이지에서 확인
SUPABASE_URL=https://your-project.supabase.co

# Project Settings → API → Service Role Key (절대 클라이언트에 노출 금지)
SUPABASE_SERVICE_KEY=eyJxxxxx...
